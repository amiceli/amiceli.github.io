pre-commit:
  parallel: true
  commands:
    biome:
      run: "npm run lint"
      include: "src/**"

commit-msg:
  commands:
    commitlint:
      run: "npx --no-install commitlint --edit $1"
      include: ".git/COMMIT_EDITMSG"
